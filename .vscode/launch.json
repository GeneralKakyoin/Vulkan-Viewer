{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run viewer_app",
      "type": "lldb",
      "request": "launch",
      "cargo": {
        "args": ["build", "-p", "viewer_app"]
      },
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}