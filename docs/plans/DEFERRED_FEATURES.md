# DEFERRED_FEATURES.md

Canonical list of features that were considered during milestone planning but deferred because they were too early.

Use this file to avoid losing useful scope that should be revisited later.

## Rules

1. Add an entry any time planning defers a feature as "too early."
2. Link the exact plan/review where the deferral happened.
3. Keep entries after promotion; set status to `promoted` and link the plan that adopted it.
4. Use `dropped` only when the user or an approved plan/review explicitly cancels the feature.

## Deferred feature list

| Candidate feature | Considered in | Why deferred as too early | Earliest reconsideration milestone | Dependency or trigger | Status | Notes/links |
| --- | --- | --- | --- | --- | --- | --- |
| Baseline image-diff harness for screenshot captures | `docs/plans/Retired/PLAN_test_automation_flags.md` | Current scope adds capture only; diffing needs fixture baselines, tolerances, and flaky-platform strategy | Next `U` testing-automation milestone | Stable deterministic scene presets and baseline management policy | promoted | Implemented via `viewer_app` binary `screenshot_diff` (current workspace task) |
| Waypoint/script file camera paths for automated verification | `docs/plans/Retired/PLAN_test_automation_flags.md` | Current scope keeps camera path env-configured and deterministic; file-driven scripts add format/design surface | Next `U` testing-automation milestone | Need agreed script format and ownership (`viewer_app` vs tooling) | promoted | Implemented via `VIEWER_TEST_CAMERA_PATH_FILE` JSON waypoint support (current workspace task) |
| Full per-face material/texture decode from object update payloads | `docs/plans/PLAN_N03.md` | N03 is bounded decode/lifecycle expansion only; full material payload parity would over-expand scope and overlap with material milestones | `A06` | Stable bounded object-feed path from N03 plus active material descriptor pipeline ownership | open | Keep N03 focused on feed and lifecycle, not full material parity |
| Deep object-cache miss/retry parity for `ObjectUpdateCached` | `docs/plans/PLAN_N03.md` | Requires broader cache protocol behavior and recovery policy beyond N03 bounded ingest goals | `N07` | Confirmed object-feed baseline from N03 and agreed cache-miss policy/ownership | open | N03 should only include bounded cached-update handling sufficient for lifecycle continuity |
| Multi-region object continuity/handoff semantics from region-transition control traffic | `docs/plans/PLAN_N03.md` | N03 targets first-region bounded object feed; multi-region continuity belongs to dedicated continuity milestone | `N07` | Repeated live observation and typed transition-state model for `CrossedRegion`/`ConfirmEnableSimulator` | open | Track with region-transition continuity work, not N03 |
| In-app login/credential UI (username/password/mfa entry) | `docs/plans/PLAN_U04.md` | U04 focuses on shell alignment and status clarity; credential entry adds security/UX surface area and broader workflow decisions | `U09` | Stable U04 shell + explicit security and UX decisions for credential storage/entry | open | Keep U04 as display/feedback only |
| Persistent egui layout save/restore or docking adoption | `docs/plans/PLAN_U04.md` | Adds complexity and long-term UX surface; U04 can regroup with simple panels/tabs first | `U09` | Stable window/panel grouping and agreed persistence strategy | open | Reconsider after U04 proves daily use |
| Export/copy diagnostics bundles from UI (logs/snapshot/relay) | `docs/plans/PLAN_U04.md` | Needs format, redaction, and ownership decisions; not required for U04 workflow alignment | `U09` | Agreed diagnostics bundle format and redaction policy | open | Could become tooling task instead of UI task |
| Full teleport handoff parity (destination-region session/socket orchestration) | `docs/plans/PLAN_N07.md` | N07 is bounded continuity state and diagnostics, not complete teleport/session orchestration parity | Next network continuity milestone after `N07` | Stable N07 continuity phase model and approved transport/policy expansion scope | open | Keep N07 focused on bounded transition-state propagation |
| Unlimited region graph streaming and long-lived retention policy | `docs/plans/PLAN_N07.md` | N07 intentionally caps continuity breadth to avoid unbounded memory/state growth | `R10+` / later parity-phase network milestone | Proven bounded continuity model and approved scaling policy | open | Revisit once bounded continuity is stable in live runs |
| Parcel/estate/environment continuity semantics across region handoffs | `docs/plans/PLAN_N07.md` | Requires additional semantic decode and rendering integration beyond N07 continuity baseline | `R10+` | Region continuity baseline + approved environment/parcel contract work | open | Keep N07 scoped to transition control and region continuity summary |
| Full skeletal avatar animation graph and retargeting parity | `docs/plans/PLAN_R08.md` | R08 is bounded proxy appearance + first attachment rendering, not full animation-system parity | Next avatar/render parity milestone after `R08` | Stable R08 proxy lifecycle + approved animation-system scope and contracts | open | Keep R08 focused on deterministic proxy/attachment baseline |
| Full wearable/system-layer and baked-texture avatar parity | `docs/plans/PLAN_R08.md` | Requires wider asset/material/avatar pipeline breadth than bounded R08 scope | `A10+` / later avatar parity milestone | Stable R08 surface contracts + continuity-aware asset policy from A10 | open | Revisit after R08 and A10 are stable |
| Full attachment slot/category parity with dynamic streaming lifecycle | `docs/plans/PLAN_R08.md` | R08 intentionally limits attachment set size and semantics to bounded deterministic behavior | Next avatar/render expansion milestone | Validated bounded attachment proxy baseline in live and stress verification | open | Expand only after baseline lifecycle proves stable |
| Advanced docking/layout workspace system beyond bounded panel persistence | `docs/plans/PLAN_U09.md` | U09 focuses workflow clarity and bounded shortcuts; full layout system introduces broad UX/state complexity | Later UX platform milestone after `U09` | Stable U09 workflow usage data and agreed persistence ownership strategy | open | Keep U09 focused on status clarity and action flow |
| Full diagnostics export/redaction pipeline integrated into UI | `docs/plans/PLAN_U09.md` | Requires formal bundle schema, redaction policy, and tooling ownership decisions outside U09 workflow scope | Later U/tooling milestone | Approved diagnostics bundle format, redaction policy, and ownership boundary | open | Could be split into CLI/tool + UI trigger milestone |
| Predictive multi-hop region asset prefetch beyond active/previous/neighbor scope | `docs/plans/PLAN_A10.md` | A10 is bounded continuity-window policy only; multi-hop prediction adds unbounded policy complexity and broader ownership decisions | Later asset/network continuity milestone after `A10` | Stable A10 bounded retention/promotion/eviction behavior and approved predictive policy model | open | Keep A10 deterministic and bounded to immediate continuity scopes |
| Cross-session persistent warm cache with long-lived residency policy | `docs/plans/PLAN_A10.md` | A10 targets bounded in-session continuity windows; persistent warm cache introduces storage lifecycle and invalidation complexity | Later platform/asset cache milestone | Proven A10 in-session policy + explicit persistence/invalidation ownership and budget model | open | Reconsider after bounded continuity cache discipline is validated |
| Full live capability/CDN asset-class parity beyond bounded continuity verification | `docs/plans/PLAN_A10.md` | A10 intentionally avoids full transport/parity breadth to keep cache policy bounded and reviewable | Later parity-phase asset milestone | Stable A10 bounded policy and approved expanded transport/parity scope | open | Expand incrementally by asset class after A10 baseline |
| Teleport destination-session orchestration parity | `docs/plans/PLAN_N11.md` | N11 is bounded handoff hardening/diagnostics; full teleport session orchestration adds major transport + policy breadth | Later network continuity milestone after `N11` | Stable N11 degraded/healthy handoff classification and approved teleport orchestration scope | open | Keep N11 focused on deterministic handoff state and observability |
| Multi-hop region transition prediction beyond bounded continuity state | `docs/plans/PLAN_N11.md` | Predictive transition modeling increases state/policy complexity beyond N11 hardening scope | Later network optimization milestone after `N11` | Proven bounded transition timers and recovery behavior in live observation | open | Add only after N11 baseline is stable |
| Full EEP day-cycle and sky/water parity system | `docs/plans/PLAN_R12.md` | R12 is a bounded atmosphere baseline; full EEP parity introduces substantial shader/system scope | Later render parity milestone after `R12` | Stable R12 environment contracts and approved EEP scope split | open | Keep R12 focused on deterministic readability baseline |
| Advanced post-processing and atmospheric scattering framework | `docs/plans/PLAN_R12.md` | Requires broader render pipeline restructuring not justified by R12 baseline goals | Later rendering polish/parity milestone | Stable R12 baseline and agreed post-processing architecture ownership | open | Defer cinematic stack until baseline is proven |
| Full multi-class live asset streaming parity | `docs/plans/PLAN_A13.md` | A13 intentionally targets bounded first live-asset slice; full parity would over-expand transport/cache scope | Later asset parity milestone after `A13` | Stable A13 source-selection and cache-admission behavior | open | Expand by asset class incrementally |
| Persistent warm cache with disk invalidation/versioning policy | `docs/plans/PLAN_A13.md` | Introduces storage lifecycle and invalidation semantics beyond A13 bounded bridge | Later platform/asset cache milestone | Proven A13 live ingestion policy and explicit persistence ownership model | open | Revisit after in-session live bridge stabilizes |
| Full diagnostics bundle export + redaction pipeline | `docs/plans/PLAN_U14.md` | Requires schema/redaction/tooling ownership decisions beyond bounded workflow resilience scope | Later UX/tooling milestone after `U14` | Stable U14 operator controls and agreed export security policy | open | Candidate split into CLI tooling + UI trigger later |
| Workspace docking/persistent layout profiles | `docs/plans/PLAN_U14.md` | Broader UI state architecture and persistence complexity exceeds U14 bounded recovery goals | Later UX platform milestone | Stable U14 workflow usage and agreed layout ownership strategy | open | Keep U14 focused on resilience controls and messaging |
| Live continuity probe retry command wiring from recovery controls | `docs/plans/PLAN_U14.md` | U14 intentionally left retry probe unavailable until bounded live worker command path was implemented | `N15` | Typed command lane in `viewer_app` + bounded probe API in `viewer_net` with cooldown/single-flight semantics | promoted | Implemented in `docs/plans/PLAN_N15.md` and `docs/reports/REPORT_N15.md` |
| Full app-camera and control-flag parity for live `AgentUpdate` messages | `docs/plans/PLAN_OBJECT_INGRESS_AGENT_UPDATE_CADENCE_2026-03-29.md` | The current object-ingress slice only needs bounded worker-owned interest-state cadence; a full camera/control contract would widen scope across worker/app boundaries before ingress is restored | Later network/live-parity milestone after object ingress is non-zero | Stable first-region object ingress plus an approved cross-thread camera/control ownership design | open | Keep the current slice focused on restoring object ingress, not full movement parity |
| Standalone `PacketAck` flush scheduling and `StartPingCheck` / `CompletePingCheck` parity | `docs/plans/PLAN_OBJECT_INGRESS_ACK_TRAILER_PARITY_2026-03-29.md` | The current object-ingress slice is limited to ACK-trailer parity on existing outbound first-simulator messages; a standalone control scheduler would widen scope and repeats an area that already regressed startup when approximated naively | Later network/live-parity milestone after ACK-trailer parity is validated | Either object ingress still remains zero after ACK-trailer parity, or live capture on the blocked path begins showing `StartPingCheck` without broader ingress | open | Reconsider only with stronger live evidence; do not reintroduce naive standalone reliability replies |
| Blind mirroring of Firestorm pre-burst housekeeping/control requests (`AgentAnimation`, `SetAlwaysRun`, `MuteListRequest`, `MoneyBalanceRequest`, `AgentDataUpdateRequest`) | `docs/plans/PLAN_OBJECT_INGRESS_RUNTIME_SOCKET_FORENSICS_2026-03-30.md` | The preserved pcaps show the blocked app path may still have unresolved runtime local-port behavior on the same simulator endpoint, so copying these Firestorm requests now would risk chasing a secondary symptom before the active socket lifecycle is proven | Later network/live-parity milestone after runtime socket continuity is proven or falsified | Connected runtime diagnostics show a single coherent local-port lifecycle and object ingress is still zero | promoted | Narrowed and promoted into `docs/plans/PLAN_OBJECT_INGRESS_POST_AMC_REQUEST_PARITY_2026-03-30.md` for the startup-specific subset (`MuteListRequest`, `MoneyBalanceRequest`, `AgentDataUpdateRequest`) |
| Full startup mirroring of Firestorm `AgentAnimation` and `SetAlwaysRun` behavior | `docs/plans/PLAN_OBJECT_INGRESS_POST_AMC_REQUEST_PARITY_2026-03-30.md` | The current bounded slice targets startup requests that Firestorm sends directly from the startup block; `AgentAnimation` and `SetAlwaysRun` belong to broader agent/movement state and would widen scope prematurely | Later network/live-parity milestone after the startup-request subset is validated | Either the narrowed request-parity slice leaves object ingress at zero, or stronger live evidence shows these broader state messages are the next smallest justified gap | open | Use Firestorm references in `reference/firestorm/indra/newview/llagent.cpp` and the preserved pcap pair before promoting |
| Full `LayerData` decode/render parity | `docs/plans/PLAN_OBJECT_INGRESS_POST_AMC_REQUEST_PARITY_2026-03-30.md` | The current bounded slice only needs to surface `LayerData` in diagnostics to distinguish received terrain/layer traffic from missing object feed; actual terrain decode/render is a separate feature surface | Later network/render parity milestone after object ingress is understood | `LayerData` is confirmed in live runs and there is an approved ownership/design path for terrain representation | open | Keep this slice classification-only in `viewer_net`; no terrain rendering work yet |
| Full Firestorm control-block mirroring in one step (`AgentHeightWidth`, `AgentAnimation`, `SetAlwaysRun`, and adjacent ACK timing) | `docs/plans/PLAN_OBJECT_INGRESS_CONTROL_BLOCK_PARITY_2026-03-30.md` | The new Firestorm pcap reveals a richer pre-burst control block, but promoting the whole bundle at once would blur causality and repeat the repo's earlier over-broad parity experiments | Later network/live-parity milestone after the first `AgentHeightWidth` slice is validated | Either `AgentHeightWidth` alone fails to change object ingress, or new live evidence shows a tighter adjacent pair is the next smallest justified step | open | Keep the first promotion to `AgentHeightWidth` only; defer the rest until the next revision |
| Broad ACK/control behavior changes before transcript-style observability is in place | `docs/plans/PLAN_OBJECT_INGRESS_ACK_AND_RECEIVE_FORENSICS_2026-03-30.md` | ACK behavior has already shown it can regress startup when approximated casually, and the remaining blocker now spans ACK timing, receive surfacing, `RegionHandshake`, and ordering concerns | Later network/live-parity milestone after the new observability slice is validated | Bounded ACK/control/receive transcript evidence clearly identifies one smallest justified behavior change | open | Do observability first; do not jump straight to another ACK behavior patch |
